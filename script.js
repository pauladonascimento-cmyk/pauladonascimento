const menuButton=document.querySelector(".menu-toggle");
const nav=document.querySelector(".main-nav");
if(menuButton&&nav){menuButton.addEventListener("click",()=>{const open=nav.classList.toggle("open");menuButton.setAttribute("aria-expanded",String(open));});}
document.querySelectorAll(".reveal").forEach(el=>{new IntersectionObserver(([entry],obs)=>{if(entry.isIntersecting){entry.target.classList.add("visible");obs.disconnect();}},{threshold:.12}).observe(el);});
document.querySelectorAll(".whatsapp-form").forEach(form=>{form.addEventListener("submit",e=>{e.preventDefault();const data=new FormData(form);const interest=form.dataset.interest||"Contato pelo site";const lines=["Olá, Paula. Vim pelo seu site.","",`Meu nome: ${data.get("nome")||""}`,`Meu WhatsApp: ${data.get("telefone")||""}`,data.get("email")?`Meu e-mail: ${data.get("email")}`:"",`Tenho interesse em: ${interest}`,"",`Mensagem: ${data.get("mensagem")||""}`].filter(Boolean);window.open(`https://wa.me/5521999791330?text=${encodeURIComponent(lines.join("\n"))}`,"_blank","noopener,noreferrer");});});
